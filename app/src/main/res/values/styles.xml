<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="CustomShapeOVerlay" parent="Widget.Material3.FloatingActionButton.Primary">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">45dp</item>
        <item name="cornerSizeTopRight">45dp</item>
        <item name="cornerSizeBottomRight">45dp</item>
        <item name="cornerSizeBottomLeft">45dp</item>
    </style>

    <style name="CustomProgressDialog" parent="MaterialAlertDialog.Material3">
        <!-- Background color of the dialog window -->
        <item name="android:backgroundTint">@color/bg_dark</item>
        <!-- Text color -->
        <item name="android:textColor">@color/white</item>

        <item name="android:windowFullscreen">false</item>
    </style>

    <style name="ThemeOverlay.App.MaterialAlertDialog" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="colorPrimary">@color/dialog_box_bg</item>
        <item name="colorSecondary">@color/dialog_box_bg</item>
        <item name="colorSurface">@color/dialog_box_bg</item>
        <item name="colorOnSurface">@color/dialog_box_bg</item>
        <item name="alertDialogStyle">@style/MaterialAlertDialog.App</item>
        <item name="materialAlertDialogTitleTextStyle">@style/MaterialAlertDialog.App.Title.Text</item>
        <item name="buttonBarPositiveButtonStyle">@style/Widget.App.Button</item>
        <item name="buttonBarNegativeButtonStyle">@style/Widget.App.Button</item>
        <item name="buttonBarNeutralButtonStyle">@style/Widget.App.Button</item>
    </style>

    <style name="MaterialAlertDialog.App" parent="MaterialAlertDialog.Material3">
        <item name="shapeAppearance">@style/ShapeAppearance.App.MediumComponent</item>
        <item name="shapeAppearanceOverlay">@null</item>
    </style>

    <style name="MaterialAlertDialog.App.Title.Text" parent="MaterialAlertDialog.Material3.Title.Text">
        <item name="android:textColor">@color/grey2</item>
    </style>

    <style name="Widget.App.Button" parent="Widget.Material3.Button.TextButton.Dialog">
        <item name="android:textColor">@color/my_green</item>
    </style>

    <style name="ShapeAppearance.App.MediumComponent" parent="ShapeAppearance.Material3.MediumComponent">
        <item name="cornerFamily">cut</item>
        <item name="cornerSize">0dp</item>
    </style>

</resources>